# Run Services
exec udiskie --no-automount --tray
# exec waybar

# Border
default_border pixel 4
smart_borders on

gaps inner 24
gaps outer 24

# Font
font pango:JetBrainsMono Nerd Font 8

# Mod1 for Alt.
# Mod4 for Home.
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Apps
# set $term kitty
set $term foot
set $menu wmenu-run
set $browser firefox
set $filemanager nautilus
set $displayManager wdisplays
set $discord discord

# Wallpaper
output * bg ~/dotfiles/wallpapers/sky.png fill

include /etc/sway/config.d/*
include ~/.config/sway/config.d/*
include ~/.config/sway/swayfx

set $opacity 1
for_window [class="kitty"] opacity $opacity
for_window [app_id="kitty"] opacity $opacity
